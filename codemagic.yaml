workflows:
  ios-workflow:
    name: iOS Debug Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      node: latest
      xcode: latest
    scripts:
      - name: Install dependencies
        script: |
          npm install
          npm install -g @ionic/cli
      - name: Build Web Project
        script: |
          npm run build
      - name: Capacitor Sync
        script: |
          rm -rf ios/App/App.xcworkspace
          rm -rf ios/App/Pods
          npx cap add ios || true
          npx cap sync ios
     - name: Build iOS (Debug)
        script: |
          cd ios/App
          xcodebuild -project App.xcodeproj -resolvePackageDependencies -clonedSourcePackagesPath ./packages
          xcodebuild -project App.xcodeproj -scheme App -configuration Debug -sdk iphonesimulator
    artifacts:
      - ios/App/*.xcresult
    publishing:
      email:
        recipients:
          - uticgirona@gmail.com
